spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/rate
    username: postgres
    password: Grond352
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false
